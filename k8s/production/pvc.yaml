apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prod-hub-nats-flapi-pvc
  namespace: prod-hub-nats-flapi
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: prod-hub-nats-flapi-storage
  resources:
    requests:
      storage: 100Gi
